# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AppStudTest' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AppStudTest
  pod ‘GoogleMaps’
  pod ‘GooglePlaces’
  pod 'Alamofire', '~> 4.2.0'
  pod 'SwiftyJSON'
  pod 'SDWebImage/WebP'

end
